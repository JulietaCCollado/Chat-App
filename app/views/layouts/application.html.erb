<!DOCTYPE html>
<html>
  <head>
  <title><%= content_for(:title) || "Chat App" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= javascript_importmap_tags %>

  <%= yield :head %>

  <link rel="icon" href="/icon.png" type="image/png">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.png">

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    
</head>


  <body>
  <!-- Navbar  -->
    <nav class="bg-gray-800 text-white p-4 flex justify-between items-center">
      <div class="text-xl font-bold">
        <a href="<%= root_path %>">ChatApp</a>
      </div>
      <div class="flex flex-col items-start">
        <% if user_signed_in? %>
          <span>Welcome, <%= current_user.display_name.presence || current_user.email %></span>
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "bg-red-600 px-3 py-1 rounded hover:bg-red-700" %>
        <% else %>
        <div class="flex space-x-4">
         <%= link_to "Sign In", new_user_session_path, class: "hover:underline" %>
         <%= link_to "Sign Up", new_user_registration_path, class:"hover:underline" %>
        </div>
        <% end %>
      </div>
    </nav>
<!--Flash Messages for login/logout success/errors-->
    <% if notice %>
     <div class="bg-green-100 text-green-800 p-2 rounded my-2 text-center"><%= notice %></div>
    <% end%>
    <% if alert %>
     <div class="bg-red-100 text-red-800 p-2 rounded my-2 text-center"><%= alert %></div>
    <% end %>
    <main class="max-w-4xl mx-auto p-4">
     <%= yield %>
    </main>
  </body>
</html>
